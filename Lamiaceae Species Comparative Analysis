# make protein files without isoforms
https://github.com/NBISweden/AGAT/blob/master/bin/agat_sp_keep_longest_isoform.pl
1. Filter the gff3 file with the above script
2. Use gffread with the genome fasta file and the filtered gff3 file from step 1 to generate a non redundant protein fasta file.

1. agat_sp_keep_longest_isoform.pl --gff ger.hc_gene_models.gff3  -o output.gff
2. gffread output.gff -g ger.final_asm.fa -y Teucrium_marum_pep.fa

#Orthofinder
 #remove periods from file sequences
for f in *.fa; do cp "$f" "$f.bak"; sed -i '/^>/! s/\.//g' "$f"; done

screen -L -Logfile orthofinder.log -S orthofinder
orthofinder -f ~/Lamiaceae_Species_Files/pep_files


